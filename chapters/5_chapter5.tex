\chapter{算法验证与性能评估} 

本章旨在对前文提出的非全维位置量测条件下的分布式状态观测器与鲁棒协同控制策略进行验证与性能评估。本章首先通过 MATLAB 数值仿真平台，针对四旋翼无人机编队的系统可观测性、状态估计收敛性以及机间避碰与通信连通性等关键理论指标进行严谨验证；随后，利用更贴近真实物理环境的 Gazebo 三维动力学仿真框架，为算法从理论走向实际工程部署提供进一步的验证思路与方案。

\section{MATLAB仿真实验与结果分析}
为验证所提编队控制协议的有效性，本节在 MATLAB 环境中搭建了基于欧拉数值积分法的多无人机系统仿真平台。

\subsection{仿真参数设置与拓扑配置}
仿真场景设定为三维空间中由$N=4$架无人机组成的微型集群系统，其动力学行为由公式(\ref{一阶动力学})所描述。同时，假设每架无人机所配备的底层控制器或执行器能够快速且精确地跟踪期望速度指令$\mathbf{v}_i^*$，从而支撑上层编队控制器的正常运行。

首先，验证编队局部收敛的具体配置与章节\ref{异构传感器配置实例分析}一致。无人机集群的底层通信与传感拓扑为全连通图，共包含$M=6$条双向通信链路。为了满足第3章提出的系统弱可观测性要求，对4架无人机进行了异构传感器配置，即部分无人机仅能量测特定轴向的绝对位置，而另一部分仅具备相对测距能力。系统的期望编队构型被设定为一个边长为$\sqrt{2}$m的正四面体。编队$\mathbf{p}^*$在全局坐标系下的位置分别为$\mathbf{p}_1^*=[1;1;1]$，$\mathbf{p}_2^*=[2;1;2]$，$\mathbf{p}_3^*=[1;2;2]$，$\mathbf{p}_4^*=[2;2;1]$，单位均为m。测距传感器网络与通信网络相同，均建模为无向图，由边$\mathcal{E}=\{(i,j) \mid i,j\in \mathcal{I}_{4}\}$表征。另外，仿真设置中还包含了一些增益参数，详情见表\ref{无人机集群系统刚性编队仿真基础参数表}。根据距离约束可以计算出无人机初始位置的邻域参数$\rho_p=0.29$，基于此可以对无人机的初始位置$\mathbf{p}_i(0)$进行配置，初始位置估计$\hat{\mathbf{p}}_i(0)$根据初始位置随即给出。此外，为验证系统在初始偏差下的鲁棒收敛能力，所有无人机的初始实际位置与初始估计位置均在期望位置的基础上叠加了服从均匀分布的随机扰动。此外，为了模拟真实传感器测量的噪声特性，在测距信息中引入了均值为0、方差为0.03的高斯白噪声。

\begin{table}[H]
	\centering
	\caption{无人机集群系统刚性编队仿真基础参数表}
	\label{无人机集群系统刚性编队仿真基础参数表}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabular}{lcc}
		\toprule
		\textbf{参数名称} & \textbf{符号} & \textbf{数值/配置} \\
		\midrule
		无人机数量 & $N$ & 4 \\
		通信与量测边数 & $M$ & 6 \\
		避碰安全距离下界 & $\rho_{e_1}$ & 0.5 m \\
		通信连通距离上界 & $\rho_{e_2}$ & 2.0 m \\
		期望机间距离 & $\sqrt{\beta_{ij}^*}$ & $\sqrt{2} \approx 1.414$ m \\
		仿真总时长 & $t_{\text{end}}$ & 5.0 s \\
		仿真步长频率 & $t_{\text{frequence}}$ & 1000 Hz \\
		机1量测配置 & $\tilde{C}_1$ & $\text{diag}(0, 0, 1)$ \\
		机2量测配置 & $\tilde{C}_2$ & $\text{diag}(0, 0, 0)$ \\
		机3量测配置 & $\tilde{C}_3$ & $\text{diag}(1, 1, 0)$ \\
		机4量测配置 & $\tilde{C}_4$ & $\text{diag}(1, 1, 1)$ \\
		增益参数 & $k_c,k_o$ & $k_c=5,k_o=10$ \\
		\bottomrule
	\end{tabular}
\end{table}

在验证刚性编队局部收敛之后，进一步给出四个典型编队变换场景以验证方法的实用性，其与刚性编队局部收敛实例由相同的参数配置与期望编队位置，仅是初始位置不同，具体设置见表\ref{无人机典型编队变换场景初始状态设置}，其中旋转矩阵$R$具体形式见(\ref{旋转矩阵})。

\begin{table}[htbp]
	\centering
	\caption{无人机典型编队变换场景初始状态设置}
	\label{无人机典型编队变换场景初始状态设置}
	\renewcommand{\arraystretch}{1.5}
	\begin{tabular}{c c c}
		\toprule
		\textbf{场景编号} & \textbf{变换类型} & \textbf{初始位置参数设置} $\mathbf{p}_i(0)$ \\
		\midrule
		1 & 平移 & $\mathbf{p}_i(0) = 
		\mathbf{p}_i^*-[5;5;5] , i \in \mathcal{I}_{4} $ \\
		2 & 旋转 & $\mathbf{p}_i(0) =  R(\frac{\pi}{2},\frac{\pi}{2},\frac{\pi}{2})\mathbf{p}_i^*,i \in \mathcal{I}_{4}$\\
		3 & 扩张 & $\mathbf{p}_i(0) =
		0.5 \mathbf{p}_i^* ,i \in \mathcal{I}_{4}$ \\
		4 & 收缩 & $\mathbf{p}_i(0)
		= 1.5 \mathbf{p}_i^*, i \in \mathcal{I}_{4}$\\
		\bottomrule
	\end{tabular}
\end{table}


\subsection{仿真结果与分析}
基于上述配置，经过MATLAB数值仿真计算对应的轨迹数据。其中，图\ref{基于非全维位置与距离量测的无人机编队位置轨迹}绘制了四台无人机从初始位置$\mathbf{p}_i(0)$收敛至期望位置$\mathbf{p}_i^*$的轨迹。图中的六条虚线表示无人机之间的通信和传感器网络的拓扑结构，四条实线表示每台无人机的真实轨迹。另外，为了剖析无人机编队的收敛过程，图\ref{基于非全维位置与距离量测的无人机编队位置轨迹三视图}中的三个子图分别绘制了无人机编队空间轨迹的三个视角下的收敛曲线。从轨迹形态分析，无人机在起始阶段表现出明显的非线性规避动作，而非直接指向目标点，这反映了控制器中基于奇异势场梯度的“形成项”与比例反馈“跟踪项”之间的动态竞争逻辑。在演化初期，由于初始位姿偏差较大，势场函数中$\gamma_{ij}$产生的梯度力占主导，引导无人机优先修正机间几何约束；随着估计偏差减小，跟踪项逐渐发挥主要作用，驱动集群整体平滑向全局坐标系下的期望位姿靠拢。

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\linewidth]{images/0330/1}
	\caption{基于非全维位置与距离量测的无人机编队位置轨迹}
	\label{基于非全维位置与距离量测的无人机编队位置轨迹}
\end{figure}
\begin{figure}[H]    % 常规操作\begin{figure}开头说明插入图片
		\centering            % 前面说过，图片放置在中间
		\subfloat[正视图]   % 第一张子图的下标（注意：注释要写在[]中括号内）
		{
			%\label{fig:subfig1}
			\includegraphics[height=5cm, keepaspectratio]{images/0330/2}
			% \label{}命令为每个子图添加标签，方便在正文中引用。如果你不需要引用的话，也可以不加这个命令，写法在下面有：
			% \label{}命令的{}内第一个{}中的内容fig:subfig1就是你插入的这张子图的标签，注意每个标签都不能一样，要用合适的编号去区分，比如1、2、3......
			% \label{}命令中{}内\includegraphics[]{}就是真正插入图片的命令，[]中的是图片的一些参数，{}就是图片的相对路径
			% width=0.4\textwidth 就是设置图片的大小，这里设置的是文档宽度（\textwidth）的0.4倍，在设置时注意不要超宽，不然会报错，大家多设置几个数尝试一下就能理解了
		}
		\subfloat[侧视图]
		{
			%\label{fig:subfig2}
			\includegraphics[height=5cm, keepaspectratio]{images/0330/3}
		}
		\subfloat[俯视图]
		{
			%\label{fig:subfig2}
			\includegraphics[height=5cm, keepaspectratio]{images/0330/4}
		}
		\caption{基于非全维位置与距离量测的无人机编队位置轨迹三视图}    % 整个图片的说明，注释写在{}内
		\label{基于非全维位置与距离量测的无人机编队位置轨迹三视图}            % 整个图片的标签编号，注意这里跟子图是一样的道理，标签不能重复 
	\end{figure}

针对复杂电磁环境下的通信与碰撞风险，图\ref{无人机间的相对距离演化收敛曲线}绘制了四台无人机间的距离演变曲线，可以看出在5s的仿真周期内，所有机间距离均严格限制在$[0.5\text{m}, 2.0\text{m}]$的预设区间内，有效验证了改进势场函数在边界处的奇异特性。即便在高斯噪声干扰下，距离曲线仍能稳定收敛至期望值 $\sqrt{2}m$ 。这种“硬约束”保障主要得益于公式 (\ref{gamma函数}) 中对数项的设计：当距离趋近临界值时，假设梯度力趋于无穷大，从而在控制层面上为集群提供了物理性的避碰与连通性保护，消除了动力学饱和前的安全隐患。然而，在真实场景中受物理约束影响，该设计假设不成立。本文的方法主要是设计梯度控制能够以无人机的最大驱动力和任务权重稳定在安全边界内。


\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\linewidth]{images/0330/5}
	\caption{无人机间的相对距离演化收敛曲线}
	\label{无人机间的相对距离演化收敛曲线}
\end{figure}

分布式观测器的性能是本研究的核心指标，图\ref{基于非全维位置与距离量测的无人机编队估计误差曲线}和图\ref{基于非全维位置与距离量测的无人机编队控制误差曲线}分别绘制了四台无人机的编队位置估计误差与控制误差曲线，可以看出估计误差和位置误差能够在局部趋近于零。注意到编队收敛期间会存在微小的抖动，这是由高斯量测噪声所产生的误差已验证控制方法的鲁棒性，当测距误差为零时，相应的估计误差和控制误差均会收敛至零。此外，在无人机1的$z$维位置、无人机3的$x$维和$y$维位置、无人机4的全维位置拥有直接的量测信息，因此这些无人机对应维度的估计误差收敛速度更快且在噪声环境下表现出极强的鲁棒性。通过这一现象可以看出QR分解筛选出的关键状态测量值能够利用无穷小刚性拓扑的关联性，将绝对位置信息在集群内高效扩散，从而实现了即便在严重传感器受限情况下的全局状态一致性估计。

\begin{figure}[H]
	\centering
	%\includegraphics[width=3in]{fig5}
	\subfloat[无人机1的估计误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/10}
		}
	\subfloat[无人机2的估计误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/11}}
	\\
	\subfloat[无人机3的估计误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/12}}
	\subfloat[无人机4的估计误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/13}}
	\caption{基于非全维位置与距离量测的无人机编队估计误差曲线}
	\label{基于非全维位置与距离量测的无人机编队估计误差曲线}
\end{figure}
\begin{figure}[H]
	\centering
	%\includegraphics[width=3in]{fig5}
	\subfloat[无人机1的控制误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/6}
	}
	\subfloat[无人机2的控制误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/7}}
	\\
	\subfloat[无人机3的控制误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/8}}
	\subfloat[无人机4的控制误差曲线]{
		\includegraphics[height=4cm, keepaspectratio]{images/0330/9}}
	\caption{基于非全维位置与距离量测的无人机编队控制误差曲线}
	\label{基于非全维位置与距离量测的无人机编队控制误差曲线}
\end{figure}

如图\ref{基于非全维位置与距离量测的无人机典型编队变换轨迹}所示，本文分别对平移、旋转、扩张与收缩四种基础编队变换场景进行了仿真测试。从图中各无人机的三维空间轨迹演化可以看出，在初始状态存在位置扰动，且系统仅具备非全维位置量测的严苛条件下，四架无人机均能沿平滑、无碰撞的路径精确收敛至新的期望几何构型。这一结果不仅验证了基于分布式观测器的全维状态估计在稳态重构时的精确性，更表明了基于固定编队中心偏置量调节机制的有效性。值得注意的是，在图\ref{基于非全维位置与距离量测的无人机典型编队变换轨迹}子图(c)的扩张与图\ref{基于非全维位置与距离量测的无人机典型编队变换轨迹}子图(d)的收缩变换中，无人机间的相对距离发生了显著的尺度伸缩。得益于所设计的复合控制律中势场梯度项的硬约束作用，集群在瞬态重构过程中自主协调了机动速度，始终未触发安全距离下界与通信距离上界，严格保障了编队机动过程中的网络连通性与物理安全性。

\begin{figure}[H]
	\centering
	%\includegraphics[width=3in]{fig5}
	\subfloat[平移变换轨迹]{
		\includegraphics[height=5cm, keepaspectratio]{images/0330/15}
	}
	\subfloat[旋转变换轨迹]{
		\includegraphics[height=5cm, keepaspectratio]{images/0330/16}}
	\\
	\subfloat[扩张变换轨迹]{
		\includegraphics[height=5cm, keepaspectratio]{images/0330/17}
		\label{扩张变换轨迹}
		}
	\subfloat[收缩变换轨迹]{
		\includegraphics[height=5cm, keepaspectratio]{images/0330/18}
		\label{收缩变换轨迹}
		}
	\caption{基于非全维位置与距离量测的无人机典型编队变换轨迹}
	\label{基于非全维位置与距离量测的无人机典型编队变换轨迹}
\end{figure}

为进一步验证无人机集群在执行复杂连续任务时的动态适应能力与协同鲁棒性，图\ref{基于非全维位置与距离量测的无人机编队动态切换轨迹}展示了编队跟随虚拟中心点进行分段时变轨迹跟踪，并依次完成扩张、旋转、收缩的连续构型切换。图中黑色五角星标记了虚拟任务中心的起点与终点，彩色实线刻画了各无人机的连续真实飞行路径。由图可见，在大尺度的空间机动过程中，无人机集群能够紧密跟踪分段线性的时变参考轨迹；同时，在不同编队形态连续切换的过渡阶段，系统未出现控制发散或剧烈抖振现象，各节点轨迹平滑且协调一致。该综合仿真结果有力地证明了在异构传感与非全维量测的受限环境下，所设计的分布式状态观测器具备优异的实时状态重构能力，能够为上层协同控制器持续提供低延迟、高置信度的全维状态反馈。多无人机系统在面临大尺度空间平移与多模态构型切换的双重挑战下，依然能够维持底层的刚性拓扑约束与安全防撞边界。这充分展现了本文所提编队控制方法在应对高动态、高复杂度的真实战术场景时，具有极强的系统鲁棒性与工程应用潜力。

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\linewidth]{images/0330/untitled-1}
	\caption{基于非全维位置与距离量测的无人机编队动态切换轨迹}
	\label{基于非全维位置与距离量测的无人机编队动态切换轨迹}
\end{figure}



\section{Gazebo物理仿真平台验证}


















\section{字体效果表格}

% 公式上下不要空行，置于同一个段落下即可，否则上下距离会出现高度不一致的问题
\begin{equation}
	LRI=1\ ∕\ \sqrt{1+{\left(\frac{{\mu }_{R}}{{\mu }_{s}}\right)}^{2}{\left(\frac{{\delta }_{R}}{{\delta }_{s}}\right)}^{2}}
\end{equation}

% 列：Regular、Italic、Bold、Bold Italic
% 行：宋体、黑体、楷体、Serif、Sans Serif、Typewriter、Math

\begin{table}[htb]
	\centering
	\caption{字体效果表格}
	\begin{tabular}{@{}lllll@{}}
		\toprule
		& Regular & Bold & Italic & Bold Italic \\ \midrule
		宋体       & 宋体      & \colorbox{orange}{\textbf{宋体粗体}} & \textit{楷体}     &   \colorbox{gray}{\textbf{\textit{楷书粗斜体}}}  \\
		黑体         & {\heiti{}黑体}      & \textbf{\heiti{}黑体粗体} &   \textit{\heiti{}黑体斜体}     & \colorbox{gray}{\textit{\textbf{\heiti{}黑体粗斜体}}}   \\
		楷体         & {\kaishu{}楷书}      & \textbf{\kaishu{}楷书粗体} & \textit{\kaishu{}斜体楷体} &  \colorbox{gray}{\textbf{\textit{\kaishu{}楷书粗斜体}}}    \\
		Serif(Roman/Normal)      &    Regular    &  \textbf{Bold}  &    \textit{Italic}    &     \textbf{\textit{Bold Italic}}    \\
		Sans Serif &  \textsf{Regular}       &  \textbf{\textsf{Bold}}    &  \textit{\textsf{Bold}}   &    \textbf{\textit{\textsf{Bold}}}   \\
		% 有些字体缺少特定变体，LaTeX会抛出警告，同时尽量替换为相近变体。
		% 若编译结果能接受，可忽略之。
		% 例如此处Typewriter代表的Latin Modern Sans Typewriter（lmtt）字体没有bold extended italic（bx/it）变体，
		% 所以`\textbf{\textit{\texttt{…}}}`会被替换为bold slant（b/sl）变体，并引发如下警告。（其中TU指字体编码）
		% Font shape `TU/lmtt/bx/it' in size <…> not available. Font shape `TU/lmtt/b/sl' tried instead.
		Typewriter &  \texttt{Regular}       &  \textbf{\texttt{Bold}}    &  \textit{\texttt{Bold}}   &    \textbf{\textit{\texttt{Bold}}}   \\
		Math       &   $\mathnormal{Regular} \mathrm{Roman}$  & $\mathbf{Bold}$   &    $\mathit{Italic}$    &  $\mathbf{\mathit{Bold Italic}}$    \\ \bottomrule
	\end{tabular}
\end{table}

\begin{itemize}[nosep]
	\item \colorbox{orange}{宋体粗体}在 Windows 下会成为黑体。这是因为 Windows 的中易宋体没有粗体字重而进行的妥协。
	如果想要获得宋体粗体的样式，请在配置中开启伪粗体选项。
	\item \colorbox{gray}{粗斜体}的效果是因操作系统字体而定的，中文写作中不会使用这种字形，可以忽略。
\end{itemize}

\textit{有关公式与上下文间距的一些注意事项：请保证源码中的公式的环境（如}
\\ \verb|\begin{equation}|
	\textit{）与上一段落不要有空行。否则，公式和上文段落之间会有额外的空白。}
	
	
	\section{常见问题和疑难解答}
	
	如果您遇到\href{https://bithesis.bitnp.net/faq/char-missing.html}{生僻字无法显示}、
	\href{https://bithesis.bitnp.net/faq/enumitem-nosep.html}{列表项间距过大}、
	\href{https://bithesis.bitnp.net/faq/longtable.html}{三线表需要跨页}等问题，
	请参考\textcolor{magenta}{\href{https://bithesis.bitnp.net/faq/}{在线文档的「疑难杂症」部分}}。
	
	\section{代码片段}
	
	\begin{lstlisting}[language=Python, caption={Python Code}, label={lst:pythonfile}]
		import numpy as np
		
		def incmatrix(genl1,genl2):
		m = len(genl1)
		n = len(genl2)
		M = None #to become the incidence matrix
		VT = np.zeros((n*m,1), int)  #dummy variable
		
		#compute the bitwise xor matrix
		M1 = bitxormatrix(genl1)
		M2 = np.triu(bitxormatrix(genl2),1)
		
		for i in range(m-1):
		for j in range(i+1, m):
		[r,c] = np.where(M2 == M1[i,j])
		for k in range(len(r)):
		VT[(i)*n + r[k]] = 1;
		VT[(i)*n + c[k]] = 1;
		VT[(j)*n + r[k]] = 1;
		VT[(j)*n + c[k]] = 1;
		
		if M is None:
		M = np.copy(VT)
		else:
		M = np.concatenate((M, VT), 1)
		
		VT = np.zeros((n*m,1), int)
		
		return M
	\end{lstlisting}

